# Survival Sim — הנחיות פרויקט

משחק סימולציית הישרדות דורית בתלת־ממד. האפיון המלא ב־`docs/SPEC.md` — קרא אותו לפני כל שינוי מבני.

## כלל הברזל

**שכבת `src/sim` אינה יודעת שקיים תלת־ממד.**

- אסור `import` של `react`, `three`, `@react-three/*` או כל דבר מ־`src/render` בתוך `src/sim`.
- אסור ל־`src/render` לשנות state. הוא קורא ומצייר בלבד.
- אם פונקציה ב־`sim` נוגעת ב־mesh, position של אובייקט גרפי, או ב־DOM — היא במקום הלא נכון.

בדיקה שחייבת לעבור: `npm run test:sim` מריץ 200 ימי משחק ב־Node, בלי דפדפן ובלי רינדור.

## מבנה

```
src/sim/      TypeScript טהור — הסימולציה
src/render/   R3F — ציור בלבד
src/ui/       React רגיל — יומן, חנות, עץ משפחה
src/store/    Zustand — הגשר
docs/SPEC.md  האפיון
```

## דטרמיניזם

כל אקראיות עוברת דרך `src/sim/rng.ts` עם seed. אסור `Math.random()` בשום מקום ב־`sim`.
אותו state + אותו seed + אותו מספר ticks = אותה תוצאה בדיוק. זה נבדק אוטומטית.

## כללי עבודה

- **כל מערכת חדשה מגיעה עם רשומות היומן שלה באותו commit.** מערכת שלא מייצרת טקסט ביומן אינה מוכנה.
- עובדים שלב אחר שלב לפי `docs/SPEC.md`. לא מתחילים שלב לפני שתנאי המעבר של הקודם עובר.
- placeholder גרפי (קפסולה, קובייה) הוא תקין ורצוי עד שלב 6. לא מבזבזים זמן על מודלים.
- אין `any` ב־`src/sim`. טיפוסים מלאים.
- כשמשנים איזון — מריצים את `test:sim` ומדפיסים השוואה לפני/אחרי.

## סגנון

- הערות בקוד באנגלית, טקסט משחק בעברית.
- מספרי איזון בקובץ `src/sim/balance.ts` בלבד, לא מפוזרים בקוד.
- קומיטים קטנים, אחד לכל יחידת עבודה.
